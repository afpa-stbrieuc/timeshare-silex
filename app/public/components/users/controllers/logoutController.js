'use strict';

angular.module('TimeShareSilex')

.controller('logoutCtrl', ['$location', 'userAuth', function($location, userAuth) {

	// var XXX = this;

	// XXX.submit = function() {
	// 	userAuth
	// 		.logout();
	// 	.then(function()) {
	// 		$location.path('/login');
		}
	}
}]);